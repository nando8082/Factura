import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'facturero-ups',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
